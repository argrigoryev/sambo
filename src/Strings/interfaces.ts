import { LocalizedStringsMethods } from 'react-localization';

export interface IStrings extends LocalizedStringsMethods {
  test: string;
}

export interface IMenuStrings {
  about_command: string;
  hide_command: string;
  hide_others_command: string;
  show_all_command: string;
  quit_command: string;
  edit_command: string;
  undo_command: string;
  redo_command: string;
  cut_command: string;
  copy_command: string;
  paste_command: string;
  select_all_command: string;
  view_command: string;
  toggle_full_screen_command: string;
  window_command: string;
  minimize_command: string;
  close_command: string;
  bring_all_to_front_command: string;
  file_command: string;
  open_command: string;
  help_command: string;
  moscow_site_command: string;
  russian_site_command: string;
  contact_us_command: string;
  report_problem_command: string;
}
